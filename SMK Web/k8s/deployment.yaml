apiVersion: apps/v1
kind: Deployment
metadata:
  name: smk-web
  labels:
    app: smk-web
spec:
  replicas: 2
  selector:
    matchLabels:
      app: smk-web
  template:
    metadata:
      labels:
        app: smk-web
    spec:
      containers:
        - name: smk-web
          image: svenroh09/smk-web:latest
          ports:
            - containerPort: 80
          resources:
            requests:
              memory: "32Mi"
              cpu: "50m"
            limits:
              memory: "64Mi"
              cpu: "100m"
          readinessProbe:
            httpGet:
              path: /
              port: 80
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /
              port: 80
            initialDelaySeconds: 10
            periodSeconds: 20
